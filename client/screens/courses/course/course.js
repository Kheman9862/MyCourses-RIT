import React from "react";
import { View, Text } from "react-native";

const upcoming = (props) => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>This is single course page</Text>
    </View>
  );
};

export default upcoming;
